# Fichiers journaliers Render en plus des all.log / all-json.log par défaut du collecteur.
template(name="RenderDaily" type="string"
         string="/var/log/render-%$YEAR%-%$MONTH%-%$DAY%.log")

*.* ?RenderDaily
